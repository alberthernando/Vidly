@model vidly.Models.Movie

@{
    ViewBag.Title = Model.Name;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>
<ul>
    <li>@Model.Genre.Name</li>
    @if (Model.ReleaseDate != null)
    {
        <li>@Model.ReleaseDate.Value.ToShortDateString()</li>
    }
</ul>